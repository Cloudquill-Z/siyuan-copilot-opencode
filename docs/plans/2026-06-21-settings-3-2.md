# Settings Center 3.2 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 修复导出署名与并发任务设置，并交付层次清晰的 SiYuan 原生设置中心。

**Architecture:** 将设置值转换和并发判断提取为可测试纯函数；所有导出入口在操作时刷新设置；设置页保留现有功能组件，仅重构壳层、导航、标题和统一视觉规则。

**Tech Stack:** Svelte 4、TypeScript、SCSS、SiYuan B3 主题变量、Node 回归脚本。

---

### Task 1: 回归测试

- 扩展 `scripts/test-regressions.ts`，覆盖数字选项、并发上限、保存到笔记的最新用户名和设置页结构。
- 运行并确认测试因缺少新行为而失败。

### Task 2: 业务修复

- 新增 `src/utils/settingsBehavior.ts`。
- 修复 `form-input.svelte` 的选项类型。
- 在新建任务和发送消息入口执行并发校验及提醒。
- 在 `SaveToNoteDialog.svelte` 保存时刷新导出上下文。

### Task 3: 设置中心重设计

- 重构 `SettingsPanel.svelte` 的导航、页头、保存状态和内容壳层。
- 统一通用设置行与定制面板的间距、边框、控件尺寸和响应式布局。

### Task 4: 验证交付

- 将 `plugin.json` 与 `package.json` 统一升级到 `3.2.0`。
- 执行回归测试、工具测试、生产构建、实际界面检查和 `graphify update .`。
- 中文提交修改与验证结果。

